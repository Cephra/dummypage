{
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "scripts": {
    "start": "parcel",
    "clean": "rm -rf dist .parcel-cache",
    "build": "parcel build"
  },
  "source": [
    "src/index.html",
    "src/404.html"
  ]
}
