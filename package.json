{
  "devDependencies": {
    "@parcel/transformer-pug": "^2.12.0",
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0"
  },
  "scripts": {
    "start": "parcel",
    "clean": "rm -rf dist .parcel-cache",
    "build": "parcel build"
  },
  "source": [
    "src/index.pug",
    "src/404.pug"
  ]
}
